import {InitialUsersStateType} from "./users-reducer";

test("", () => {
        const state:InitialUsersStateType = {
            users: [],
            pageSize: 10,
            totalUsersCount: 0,
            currentPage: 1,
            isFetching: false,
            followInProgress: []
        }
        // expect()
    }
)